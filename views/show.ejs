<!DOCTYPE html> 
<html>
    <head></head>

    <body>
        <h1>Fruit Show Page</h1>

        The <%=fruit.name%> is <%=fruit.color%>.

        <% if(fruit.readyToEat === true) { %>
            It is ready to Eat.
        <% } else { %>
            It is not ready to eat.
        <% } %>
    </body>
</html>